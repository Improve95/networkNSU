package ru.improve.communication;

import ru.improve.communication.sender.BufferSender;

import java.nio.ByteBuffer;
import java.nio.channels.SocketChannel;

public class BufferSenderReceiver {

    public static void sendBuffer(SocketChannel socketChannel, ByteBuffer buffer,
                                  BufferSender bufferSender) {

    }

    public static void receiveBuffer() {
        //хз, зачем мне это
    }
}
